Meetup Shipping Module
==========

The module shown in the last meetup:

To enable it run the following commands:

```
bin/magento module:status
bin/magento module:enable Meetup_Shipping
bin/magento setup:upgrade
```
